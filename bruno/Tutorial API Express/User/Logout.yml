info:
  name: Logout
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3333/auth/logout
  body:
    type: json
    data: |-
      {
        "userId": 15
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

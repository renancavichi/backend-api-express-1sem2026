info:
  name: Refresh Token
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:3333/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

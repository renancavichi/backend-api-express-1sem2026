info:
  name: Close All Sessions
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3333/auth/close-sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

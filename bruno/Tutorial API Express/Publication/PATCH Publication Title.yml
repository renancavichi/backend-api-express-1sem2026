info:
  name: PATCH Publication Title
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/publication/title/2
  body:
    type: json
    data: |-
      {
        "title": "Uma Hiper Nova Matéria"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
